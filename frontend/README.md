# Smart Campus — frontend (Member 4)

React + Vite UI for **authentication (Google OAuth)**, **notifications**, **notification preferences**, and **admin user management**.

Setup and run instructions are in the **repository root** [`README.md`](../README.md) (MySQL, OAuth env vars, `npm run dev`, Maven backend).
